select movie_link.movie_id, link_type.id,  sum(movie_link.linked_movie_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.movie_id, link_type.id;
select movie_link.movie_id,  max(movie_link.movie_id) from movie_link group by movie_link.movie_id order by movie_link.movie_id ASC;
select movie_link.movie_id,  min(movie_link.movie_id) from movie_link group by movie_link.movie_id order by movie_link.movie_id DESC;
select movie_link.id,  min(movie_link.linked_movie_id) from movie_link group by movie_link.id;
select link_type.id, movie_link.linked_movie_id,  max(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by link_type.id, movie_link.linked_movie_id;
select movie_link.id,  max(movie_link.movie_id) from movie_link group by movie_link.id having max(movie_link.movie_id) > 1817;
select movie_link.movie_id,  sum(movie_link.id) from movie_link group by movie_link.movie_id order by movie_link.movie_id DESC;
select movie_link.id,  sum(movie_link.movie_id) from movie_link group by movie_link.id;
select movie_link.id,  min(movie_link.link_type_id) from movie_link group by movie_link.id;
select movie_link.id, link_type.id,  min(movie_link.linked_movie_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.id, link_type.id;
select movie_link.id,  max(movie_link.id) from movie_link group by movie_link.id;
select movie_link.linked_movie_id, link_type.id,  sum(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select link_type.id, movie_link.id,  min(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by link_type.id, movie_link.id;
select movie_link.id, link_type.id,  sum(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.id, link_type.id;
select movie_link.movie_id,  sum(movie_link.link_type_id) from movie_link group by movie_link.movie_id order by movie_link.movie_id DESC;
select link_type.id, movie_link.id,  sum(movie_link.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by link_type.id, movie_link.id;
select movie_link.linked_movie_id, link_type.id,  max(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select movie_link.linked_movie_id, link_type.id,  min(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select movie_link.movie_id,  max(movie_link.link_type_id) from movie_link group by movie_link.movie_id order by movie_link.movie_id ASC;
select movie_link.id,  min(movie_link.link_type_id) from movie_link group by movie_link.id;
select movie_link.id, link_type.id,  sum(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.id, link_type.id;
select movie_link.id,  max(movie_link.id) from movie_link group by movie_link.id having max(movie_link.id) > 1725;
select movie_link.movie_id,  min(movie_link.linked_movie_id) from movie_link group by movie_link.movie_id order by movie_link.movie_id ASC;
select movie_link.id,  min(movie_link.linked_movie_id) from movie_link group by movie_link.id having min(movie_link.linked_movie_id) <= 1872;
select movie_link.movie_id, link_type.id,  max(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select movie_link.id,  sum(movie_link.link_type_id) from movie_link group by movie_link.id;
select movie_link.id,  min(movie_link.link_type_id) from movie_link group by movie_link.id;
select movie_link.linked_movie_id, link_type.id,  max(movie_link.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.linked_movie_id, link_type.id;
select movie_link.linked_movie_id,  avg(movie_link.linked_movie_id) from movie_link group by movie_link.linked_movie_id having avg(movie_link.linked_movie_id) < 1897;
select movie_link.id,  max(movie_link.link_type_id) from movie_link group by movie_link.id;
select movie_link.movie_id, link_type.id,  min(movie_link.movie_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select link_type.id, movie_link.id,  sum(movie_link.linked_movie_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by link_type.id, movie_link.id;
select movie_link.movie_id,  min(movie_link.id) from movie_link group by movie_link.movie_id order by movie_link.movie_id DESC;
select movie_link.movie_id, link_type.id,  max(movie_link.linked_movie_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select movie_link.movie_id,  max(movie_link.id) from movie_link group by movie_link.movie_id order by movie_link.movie_id DESC;
select movie_link.id, link_type.id,  max(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.id, link_type.id;
select movie_link.movie_id, link_type.id,  sum(movie_link.movie_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.movie_id, link_type.id;
select movie_link.id, link_type.id,  sum(movie_link.movie_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.id, link_type.id;
select movie_link.id,  avg(movie_link.link_type_id) from movie_link group by movie_link.id;
select link_type.id, movie_link.id,  min(movie_link.movie_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by link_type.id, movie_link.id;
select movie_link.id, link_type.id,  sum(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.id, link_type.id;
select movie_link.movie_id, link_type.id,  max(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select movie_link.id, link_type.id,  sum(movie_link.linked_movie_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.id, link_type.id;
select movie_link.id,  avg(movie_link.linked_movie_id) from movie_link group by movie_link.id;
select movie_link.id, link_type.id,  sum(movie_link.movie_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.id, link_type.id;
select movie_link.movie_id, link_type.id,  min(movie_link.link_type_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select movie_link.id, link_type.id,  max(movie_link.movie_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.id, link_type.id;
select movie_link.id, link_type.id,  sum(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.id, link_type.id;
select movie_link.linked_movie_id, link_type.id,  sum(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select movie_link.id, link_type.id,  max(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.id, link_type.id;
select movie_link.movie_id, link_type.id,  min(movie_link.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select movie_link.movie_id, link_type.id,  max(movie_link.movie_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.movie_id, link_type.id;
select movie_link.linked_movie_id, link_type.id,  sum(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.linked_movie_id, link_type.id;
select movie_link.id,  avg(movie_link.linked_movie_id) from movie_link group by movie_link.id;
select movie_link.id, link_type.id,  min(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.id, link_type.id;
select movie_link.movie_id, link_type.id,  max(movie_link.linked_movie_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.movie_id, link_type.id;
select movie_link.movie_id, link_type.id,  sum(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.movie_id, link_type.id;
select link_type.id, movie_link.movie_id,  min(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by link_type.id, movie_link.movie_id;
select movie_link.id,  sum(movie_link.link_type_id) from movie_link group by movie_link.id;
select movie_link.id,  min(movie_link.linked_movie_id) from movie_link group by movie_link.id;
select movie_link.movie_id, link_type.id,  sum(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.movie_id, link_type.id;
select movie_link.id, link_type.id,  min(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.id, link_type.id;
select movie_link.movie_id, link_type.id,  max(movie_link.linked_movie_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.movie_id, link_type.id;
select movie_link.movie_id, link_type.id,  min(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select movie_link.id, link_type.id,  sum(movie_link.linked_movie_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.id, link_type.id;
select movie_link.id,  sum(movie_link.id) from movie_link group by movie_link.id;
select movie_link.id, link_type.id,  sum(movie_link.movie_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.id, link_type.id;
select movie_link.movie_id,  sum(movie_link.link_type_id) from movie_link group by movie_link.movie_id order by movie_link.movie_id ASC;
select movie_link.movie_id, link_type.id,  sum(movie_link.linked_movie_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select movie_link.linked_movie_id,  avg(movie_link.linked_movie_id) from movie_link group by movie_link.linked_movie_id having avg(movie_link.linked_movie_id) < 1941;
select movie_link.linked_movie_id, link_type.id,  min(movie_link.linked_movie_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select link_type.id, movie_link.linked_movie_id,  min(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by link_type.id, movie_link.linked_movie_id;
select movie_link.linked_movie_id, link_type.id,  max(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.linked_movie_id, link_type.id;
select movie_link.id,  min(movie_link.id) from movie_link group by movie_link.id;
select movie_link.linked_movie_id, link_type.id,  max(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.linked_movie_id, link_type.id;
select movie_link.id, link_type.id,  min(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.id, link_type.id;
select movie_link.id, link_type.id,  max(movie_link.movie_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.id, link_type.id;
select movie_link.linked_movie_id, link_type.id,  sum(movie_link.link_type_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select movie_link.id, link_type.id,  min(movie_link.movie_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.id, link_type.id;
select link_type.id, movie_link.id,  sum(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by link_type.id, movie_link.id;
select movie_link.movie_id,  sum(movie_link.movie_id) from movie_link group by movie_link.movie_id order by movie_link.movie_id DESC;
select movie_link.linked_movie_id, link_type.id,  sum(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select movie_link.id,  sum(movie_link.link_type_id) from movie_link group by movie_link.id;
select movie_link.id,  sum(movie_link.link_type_id) from movie_link group by movie_link.id having sum(movie_link.link_type_id) != 283;
select movie_link.linked_movie_id, link_type.id,  sum(movie_link.link_type_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select movie_link.linked_movie_id, link_type.id,  sum(movie_link.movie_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select link_type.id, movie_link.linked_movie_id,  max(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by link_type.id, movie_link.linked_movie_id;
select movie_link.movie_id,  max(movie_link.movie_id) from movie_link group by movie_link.movie_id order by movie_link.movie_id ASC;
select link_type.id, movie_link.movie_id,  max(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by link_type.id, movie_link.movie_id;
select movie_link.id,  max(movie_link.id) from movie_link group by movie_link.id;
select movie_link.linked_movie_id, link_type.id,  min(movie_link.movie_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select movie_link.movie_id,  sum(movie_link.id) from movie_link group by movie_link.movie_id order by movie_link.movie_id DESC;
select movie_link.linked_movie_id, link_type.id,  max(movie_link.movie_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select movie_link.id,  min(movie_link.link_type_id) from movie_link group by movie_link.id having min(movie_link.link_type_id) > 178;
select movie_link.id, link_type.id,  max(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.id, link_type.id;
select movie_link.movie_id,  max(movie_link.id) from movie_link group by movie_link.movie_id order by movie_link.movie_id DESC;
select movie_link.id, link_type.id,  max(movie_link.movie_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.id, link_type.id;
select movie_link.linked_movie_id,  avg(movie_link.link_type_id) from movie_link group by movie_link.linked_movie_id having avg(movie_link.link_type_id) >= 289;
select movie_link.linked_movie_id, link_type.id,  min(movie_link.movie_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select movie_link.movie_id,  max(movie_link.movie_id) from movie_link group by movie_link.movie_id order by movie_link.movie_id DESC;
select movie_link.linked_movie_id, link_type.id,  min(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select movie_link.movie_id,  max(movie_link.id) from movie_link group by movie_link.movie_id order by movie_link.movie_id DESC;
select movie_link.id, link_type.id,  max(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.id, link_type.id;
select movie_link.id, link_type.id,  min(movie_link.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.id, link_type.id;
select movie_link.id, link_type.id,  sum(movie_link.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.id, link_type.id;
select link_type.id, movie_link.movie_id,  min(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by link_type.id, movie_link.movie_id;
select movie_link.movie_id, link_type.id,  sum(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.movie_id, link_type.id;
select movie_link.movie_id, link_type.id,  min(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.movie_id, link_type.id;
select movie_link.movie_id, link_type.id,  min(movie_link.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.movie_id, link_type.id;
select link_type.id, movie_link.movie_id,  max(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by link_type.id, movie_link.movie_id;
select link_type.id, movie_link.linked_movie_id,  min(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by link_type.id, movie_link.linked_movie_id;
select movie_link.movie_id, link_type.id,  min(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select movie_link.id,  max(movie_link.linked_movie_id) from movie_link group by movie_link.id having max(movie_link.linked_movie_id) > 1882;
select movie_link.id,  avg(movie_link.movie_id) from movie_link group by movie_link.id;
select movie_link.linked_movie_id, link_type.id,  min(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select movie_link.linked_movie_id, link_type.id,  min(movie_link.link_type_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select movie_link.linked_movie_id, link_type.id,  sum(movie_link.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.linked_movie_id, link_type.id;
select movie_link.movie_id, link_type.id,  sum(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.movie_id, link_type.id;
select movie_link.id,  max(movie_link.movie_id) from movie_link group by movie_link.id;
select movie_link.linked_movie_id,  avg(movie_link.link_type_id) from movie_link group by movie_link.linked_movie_id having avg(movie_link.link_type_id) <= 180;
select movie_link.movie_id, link_type.id,  max(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select movie_link.movie_id,  sum(movie_link.linked_movie_id) from movie_link group by movie_link.movie_id order by movie_link.movie_id DESC;
select link_type.id, movie_link.movie_id,  min(movie_link.linked_movie_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by link_type.id, movie_link.movie_id;
select movie_link.linked_movie_id, link_type.id,  max(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select movie_link.id,  sum(movie_link.movie_id) from movie_link group by movie_link.id;
select movie_link.id,  min(movie_link.id) from movie_link group by movie_link.id;
select movie_link.id, link_type.id,  min(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.id, link_type.id;
select movie_link.movie_id,  min(movie_link.linked_movie_id) from movie_link group by movie_link.movie_id order by movie_link.movie_id DESC;
select movie_link.id,  avg(movie_link.movie_id) from movie_link group by movie_link.id;
select movie_link.linked_movie_id, link_type.id,  max(movie_link.movie_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select movie_link.linked_movie_id, link_type.id,  min(movie_link.linked_movie_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.linked_movie_id, link_type.id;
select movie_link.linked_movie_id, link_type.id,  max(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select movie_link.linked_movie_id,  avg(movie_link.link_type_id) from movie_link group by movie_link.linked_movie_id having avg(movie_link.link_type_id) != 293;
select movie_link.id,  avg(movie_link.movie_id) from movie_link group by movie_link.id;
select movie_link.movie_id, link_type.id,  max(movie_link.linked_movie_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select movie_link.movie_id, link_type.id,  sum(movie_link.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.movie_id, link_type.id;
select movie_link.id, link_type.id,  sum(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.id, link_type.id;
select movie_link.linked_movie_id, link_type.id,  min(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.linked_movie_id, link_type.id;
select movie_link.linked_movie_id, link_type.id,  max(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select movie_link.id, link_type.id,  sum(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.id, link_type.id;
select movie_link.id,  max(movie_link.link_type_id) from movie_link group by movie_link.id;
select movie_link.movie_id, link_type.id,  sum(movie_link.linked_movie_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.movie_id, link_type.id;
select movie_link.movie_id, link_type.id,  min(movie_link.movie_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.movie_id, link_type.id;
select movie_link.id,  sum(movie_link.linked_movie_id) from movie_link group by movie_link.id having sum(movie_link.linked_movie_id) <= 1915;
select movie_link.id, link_type.id,  sum(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.id, link_type.id;
select movie_link.id,  max(movie_link.id) from movie_link group by movie_link.id having max(movie_link.id) >= 1683;
select link_type.id, movie_link.id,  max(movie_link.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by link_type.id, movie_link.id;
select movie_link.linked_movie_id, link_type.id,  min(movie_link.linked_movie_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select movie_link.linked_movie_id,  avg(movie_link.linked_movie_id) from movie_link group by movie_link.linked_movie_id having avg(movie_link.linked_movie_id) != 1892;
select movie_link.movie_id, link_type.id,  min(movie_link.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.movie_id, link_type.id;
select movie_link.linked_movie_id, link_type.id,  min(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.linked_movie_id, link_type.id;
select movie_link.id,  max(movie_link.id) from movie_link group by movie_link.id;
select movie_link.id, link_type.id,  sum(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.id, link_type.id;
select movie_link.movie_id,  sum(movie_link.linked_movie_id) from movie_link group by movie_link.movie_id order by movie_link.movie_id DESC;
select movie_link.id, link_type.id,  min(movie_link.movie_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.id, link_type.id;
select movie_link.linked_movie_id, link_type.id,  max(movie_link.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select movie_link.movie_id, link_type.id,  max(movie_link.link_type_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select movie_link.id, link_type.id,  max(movie_link.movie_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.id, link_type.id;
select movie_link.id, link_type.id,  sum(movie_link.movie_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.id, link_type.id;
select movie_link.movie_id, link_type.id,  max(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select movie_link.linked_movie_id, link_type.id,  max(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.linked_movie_id, link_type.id;
select movie_link.linked_movie_id, link_type.id,  sum(movie_link.link_type_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.linked_movie_id, link_type.id;
select movie_link.movie_id,  max(movie_link.linked_movie_id) from movie_link group by movie_link.movie_id order by movie_link.movie_id DESC;
select movie_link.id, link_type.id,  sum(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.id, link_type.id;
select movie_link.id, link_type.id,  max(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.id, link_type.id;
select movie_link.movie_id,  sum(movie_link.id) from movie_link group by movie_link.movie_id order by movie_link.movie_id DESC;
select movie_link.linked_movie_id, link_type.id,  min(movie_link.linked_movie_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.linked_movie_id, link_type.id;
select movie_link.linked_movie_id, link_type.id,  min(movie_link.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.linked_movie_id, link_type.id;
select link_type.id, movie_link.linked_movie_id,  max(movie_link.link_type_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by link_type.id, movie_link.linked_movie_id;
select movie_link.id, link_type.id,  sum(movie_link.link_type_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.id, link_type.id;
select movie_link.linked_movie_id, link_type.id,  sum(movie_link.linked_movie_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.linked_movie_id, link_type.id;
select movie_link.movie_id, link_type.id,  min(movie_link.movie_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select movie_link.id,  min(movie_link.movie_id) from movie_link group by movie_link.id;
select movie_link.id, link_type.id,  min(movie_link.link_type_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.id, link_type.id;
select movie_link.movie_id, link_type.id,  min(movie_link.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.movie_id, link_type.id;
select movie_link.id,  min(movie_link.id) from movie_link group by movie_link.id having min(movie_link.id) >= 1712;
select movie_link.linked_movie_id, link_type.id,  max(movie_link.linked_movie_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.linked_movie_id, link_type.id;
select movie_link.id,  min(movie_link.id) from movie_link group by movie_link.id;
select movie_link.id,  avg(movie_link.linked_movie_id) from movie_link group by movie_link.id;
select movie_link.movie_id, link_type.id,  max(movie_link.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select movie_link.id, link_type.id,  sum(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.id, link_type.id;
select movie_link.id, link_type.id,  sum(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.id, link_type.id;
select movie_link.id, link_type.id,  min(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.id, link_type.id;
select movie_link.linked_movie_id, link_type.id,  min(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.linked_movie_id, link_type.id;
select movie_link.id, link_type.id,  sum(movie_link.movie_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.id, link_type.id;
select movie_link.movie_id, link_type.id,  min(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.movie_id, link_type.id;
select movie_link.movie_id, link_type.id,  max(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select movie_link.linked_movie_id,  avg(movie_link.linked_movie_id) from movie_link group by movie_link.linked_movie_id having avg(movie_link.linked_movie_id) < 1932;
select movie_link.linked_movie_id, link_type.id,  max(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select movie_link.id,  avg(movie_link.link_type_id) from movie_link group by movie_link.id;
select movie_link.linked_movie_id, link_type.id,  sum(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select link_type.id, movie_link.id,  sum(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by link_type.id, movie_link.id;
select movie_link.id, link_type.id,  sum(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.id, link_type.id;
select movie_link.id, link_type.id,  sum(movie_link.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.id, link_type.id;
select movie_link.linked_movie_id, link_type.id,  max(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select movie_link.movie_id, link_type.id,  sum(movie_link.linked_movie_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.movie_id, link_type.id;
select movie_link.id,  min(movie_link.link_type_id) from movie_link group by movie_link.id;
select movie_link.movie_id, link_type.id,  sum(movie_link.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.movie_id, link_type.id;
select movie_link.movie_id,  max(movie_link.movie_id) from movie_link group by movie_link.movie_id order by movie_link.movie_id DESC;
select movie_link.id,  min(movie_link.link_type_id) from movie_link group by movie_link.id;
select movie_link.linked_movie_id, link_type.id,  max(movie_link.link_type_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select movie_link.id,  sum(movie_link.link_type_id) from movie_link group by movie_link.id;
select movie_link.movie_id, link_type.id,  min(movie_link.linked_movie_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.movie_id, link_type.id;
select movie_link.movie_id, link_type.id,  max(movie_link.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.movie_id, link_type.id;
select movie_link.movie_id,  max(movie_link.linked_movie_id) from movie_link group by movie_link.movie_id order by movie_link.movie_id ASC;
select link_type.id, movie_link.linked_movie_id,  max(movie_link.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by link_type.id, movie_link.linked_movie_id;
select movie_link.movie_id, link_type.id,  min(movie_link.linked_movie_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.movie_id, link_type.id;
select movie_link.id,  avg(movie_link.movie_id) from movie_link group by movie_link.id;
select movie_link.id, link_type.id,  min(movie_link.linked_movie_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.id, link_type.id;
select movie_link.id,  sum(movie_link.id) from movie_link group by movie_link.id;
select movie_link.id, link_type.id,  max(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.id, link_type.id;
select movie_link.id,  sum(movie_link.linked_movie_id) from movie_link group by movie_link.id;
select movie_link.linked_movie_id, link_type.id,  sum(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.linked_movie_id, link_type.id;
select movie_link.linked_movie_id, link_type.id,  max(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.linked_movie_id, link_type.id;
select movie_link.movie_id, link_type.id,  min(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select movie_link.linked_movie_id, link_type.id,  sum(movie_link.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select movie_link.id,  min(movie_link.link_type_id) from movie_link group by movie_link.id having min(movie_link.link_type_id) < 290;
select movie_link.linked_movie_id, link_type.id,  max(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select movie_link.linked_movie_id, link_type.id,  sum(movie_link.movie_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select movie_link.id,  avg(movie_link.link_type_id) from movie_link group by movie_link.id;
select movie_link.linked_movie_id, link_type.id,  sum(movie_link.movie_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select movie_link.movie_id, link_type.id,  max(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.movie_id, link_type.id;
select movie_link.id, link_type.id,  min(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.id, link_type.id;
select movie_link.movie_id, link_type.id,  max(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select movie_link.id,  max(movie_link.link_type_id) from movie_link group by movie_link.id;
select movie_link.linked_movie_id, link_type.id,  max(movie_link.link_type_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select movie_link.linked_movie_id, link_type.id,  max(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.linked_movie_id, link_type.id;
select link_type.id, movie_link.linked_movie_id,  max(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by link_type.id, movie_link.linked_movie_id;
select movie_link.movie_id, link_type.id,  sum(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select movie_link.id,  max(movie_link.linked_movie_id) from movie_link group by movie_link.id;
select movie_link.movie_id, link_type.id,  sum(movie_link.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.movie_id, link_type.id;
select movie_link.id,  avg(movie_link.movie_id) from movie_link group by movie_link.id;
select link_type.id, movie_link.linked_movie_id,  min(movie_link.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by link_type.id, movie_link.linked_movie_id;
select movie_link.movie_id,  sum(movie_link.movie_id) from movie_link group by movie_link.movie_id order by movie_link.movie_id ASC;
select movie_link.id, link_type.id,  min(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.id, link_type.id;
select movie_link.movie_id, link_type.id,  sum(movie_link.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.movie_id, link_type.id;
select movie_link.movie_id, link_type.id,  sum(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.movie_id, link_type.id;
select movie_link.id,  sum(movie_link.movie_id) from movie_link group by movie_link.id;
select movie_link.movie_id,  sum(movie_link.id) from movie_link group by movie_link.movie_id order by movie_link.movie_id ASC;
select movie_link.movie_id,  min(movie_link.id) from movie_link group by movie_link.movie_id order by movie_link.movie_id DESC;
select movie_link.movie_id,  sum(movie_link.linked_movie_id) from movie_link group by movie_link.movie_id order by movie_link.movie_id DESC;
select movie_link.id,  min(movie_link.link_type_id) from movie_link group by movie_link.id;
select movie_link.id,  max(movie_link.id) from movie_link group by movie_link.id having max(movie_link.id) = 1699;
select movie_link.id, link_type.id,  min(movie_link.link_type_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.id, link_type.id;
select movie_link.id, link_type.id,  sum(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.id, link_type.id;
select movie_link.movie_id, link_type.id,  max(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.movie_id, link_type.id;
select link_type.id, movie_link.linked_movie_id,  max(movie_link.link_type_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by link_type.id, movie_link.linked_movie_id;
select movie_link.id,  sum(movie_link.linked_movie_id) from movie_link group by movie_link.id;
select movie_link.id, link_type.id,  min(movie_link.movie_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.id, link_type.id;
select link_type.id, movie_link.linked_movie_id,  max(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by link_type.id, movie_link.linked_movie_id;
select movie_link.movie_id, link_type.id,  max(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.movie_id, link_type.id;
select movie_link.id, link_type.id,  sum(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.id, link_type.id;
select movie_link.id,  avg(movie_link.link_type_id) from movie_link group by movie_link.id;
select link_type.id, movie_link.movie_id,  sum(movie_link.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by link_type.id, movie_link.movie_id;
select movie_link.id,  min(movie_link.link_type_id) from movie_link group by movie_link.id;
select movie_link.id,  avg(movie_link.linked_movie_id) from movie_link group by movie_link.id;
select link_type.id, movie_link.movie_id,  min(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by link_type.id, movie_link.movie_id;
select movie_link.id,  min(movie_link.id) from movie_link group by movie_link.id;
select movie_link.linked_movie_id, link_type.id,  sum(movie_link.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select movie_link.id, link_type.id,  min(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.id, link_type.id;
select movie_link.id, link_type.id,  sum(movie_link.linked_movie_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.id, link_type.id;
select movie_link.id,  avg(movie_link.link_type_id) from movie_link group by movie_link.id;
select movie_link.movie_id,  min(movie_link.linked_movie_id) from movie_link group by movie_link.movie_id order by movie_link.movie_id DESC;
select movie_link.movie_id, link_type.id,  max(movie_link.linked_movie_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select movie_link.id,  min(movie_link.movie_id) from movie_link group by movie_link.id having min(movie_link.movie_id) < 1846;
select movie_link.movie_id, link_type.id,  max(movie_link.link_type_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.movie_id, link_type.id;
select movie_link.movie_id,  min(movie_link.movie_id) from movie_link group by movie_link.movie_id order by movie_link.movie_id DESC;
select movie_link.id, link_type.id,  min(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.id, link_type.id;
select link_type.id, movie_link.linked_movie_id,  min(movie_link.linked_movie_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by link_type.id, movie_link.linked_movie_id;
select movie_link.id, link_type.id,  max(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.id, link_type.id;
select movie_link.id,  min(movie_link.linked_movie_id) from movie_link group by movie_link.id having min(movie_link.linked_movie_id) < 1927;
select movie_link.linked_movie_id, link_type.id,  sum(movie_link.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select movie_link.id,  max(movie_link.id) from movie_link group by movie_link.id having max(movie_link.id) <= 1744;
select movie_link.movie_id,  min(movie_link.link_type_id) from movie_link group by movie_link.movie_id order by movie_link.movie_id ASC;
select movie_link.movie_id,  min(movie_link.id) from movie_link group by movie_link.movie_id order by movie_link.movie_id DESC;
select movie_link.movie_id, link_type.id,  max(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select movie_link.id, link_type.id,  sum(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.id, link_type.id;
select movie_link.movie_id,  max(movie_link.id) from movie_link group by movie_link.movie_id order by movie_link.movie_id ASC;
select movie_link.linked_movie_id, link_type.id,  min(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select movie_link.movie_id, link_type.id,  max(movie_link.movie_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.movie_id, link_type.id;
select movie_link.movie_id,  min(movie_link.id) from movie_link group by movie_link.movie_id order by movie_link.movie_id ASC;
select link_type.id, movie_link.id,  min(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by link_type.id, movie_link.id;
select movie_link.id,  max(movie_link.id) from movie_link group by movie_link.id;
select movie_link.id,  sum(movie_link.link_type_id) from movie_link group by movie_link.id;
select movie_link.linked_movie_id, link_type.id,  sum(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.linked_movie_id, link_type.id;
select movie_link.movie_id, link_type.id,  min(movie_link.link_type_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select movie_link.id,  max(movie_link.link_type_id) from movie_link group by movie_link.id;
select movie_link.movie_id, link_type.id,  sum(movie_link.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select movie_link.movie_id,  sum(movie_link.id) from movie_link group by movie_link.movie_id order by movie_link.movie_id DESC;
select link_type.id, movie_link.id,  min(movie_link.movie_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by link_type.id, movie_link.id;
select movie_link.linked_movie_id,  avg(movie_link.movie_id) from movie_link group by movie_link.linked_movie_id having avg(movie_link.movie_id) > 1760;
select movie_link.movie_id, link_type.id,  min(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select movie_link.linked_movie_id, link_type.id,  max(movie_link.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.linked_movie_id, link_type.id;
select link_type.id, movie_link.movie_id,  min(movie_link.link_type_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by link_type.id, movie_link.movie_id;
select movie_link.movie_id, link_type.id,  min(movie_link.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select movie_link.id,  sum(movie_link.link_type_id) from movie_link group by movie_link.id;
select movie_link.linked_movie_id, link_type.id,  min(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select movie_link.id, link_type.id,  sum(movie_link.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.id, link_type.id;
select movie_link.id, link_type.id,  sum(movie_link.link_type_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.id, link_type.id;
select movie_link.linked_movie_id, link_type.id,  max(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select movie_link.id,  max(movie_link.id) from movie_link group by movie_link.id;
select link_type.id, movie_link.id,  sum(movie_link.link_type_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by link_type.id, movie_link.id;
select movie_link.linked_movie_id,  avg(movie_link.linked_movie_id) from movie_link group by movie_link.linked_movie_id having avg(movie_link.linked_movie_id) < 1915;
select movie_link.movie_id,  max(movie_link.id) from movie_link group by movie_link.movie_id order by movie_link.movie_id ASC;
select movie_link.id,  max(movie_link.link_type_id) from movie_link group by movie_link.id;
select movie_link.id, link_type.id,  sum(movie_link.link_type_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.id, link_type.id;
select movie_link.linked_movie_id, link_type.id,  max(movie_link.movie_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select movie_link.linked_movie_id, link_type.id,  max(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.linked_movie_id, link_type.id;
select movie_link.id,  avg(movie_link.linked_movie_id) from movie_link group by movie_link.id;
select movie_link.id,  max(movie_link.linked_movie_id) from movie_link group by movie_link.id;
select movie_link.linked_movie_id, link_type.id,  min(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.linked_movie_id, link_type.id;
select movie_link.id, link_type.id,  max(movie_link.movie_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.id, link_type.id;
select movie_link.id,  sum(movie_link.link_type_id) from movie_link group by movie_link.id having sum(movie_link.link_type_id) = 284;
select movie_link.id, link_type.id,  max(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.id, link_type.id;
select movie_link.id, link_type.id,  min(movie_link.link_type_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.id, link_type.id;
select movie_link.id,  min(movie_link.link_type_id) from movie_link group by movie_link.id;
select movie_link.id,  max(movie_link.linked_movie_id) from movie_link group by movie_link.id having max(movie_link.linked_movie_id) = 1917;
select movie_link.id,  sum(movie_link.link_type_id) from movie_link group by movie_link.id;
select movie_link.id,  sum(movie_link.movie_id) from movie_link group by movie_link.id;
select movie_link.movie_id, link_type.id,  max(movie_link.movie_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.movie_id, link_type.id;
select movie_link.id,  min(movie_link.id) from movie_link group by movie_link.id;
select movie_link.id,  avg(movie_link.linked_movie_id) from movie_link group by movie_link.id;
select movie_link.linked_movie_id, link_type.id,  sum(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select link_type.id, movie_link.linked_movie_id,  max(movie_link.linked_movie_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by link_type.id, movie_link.linked_movie_id;
select link_type.id, movie_link.linked_movie_id,  sum(movie_link.link_type_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by link_type.id, movie_link.linked_movie_id;
select movie_link.id,  max(movie_link.link_type_id) from movie_link group by movie_link.id;
select movie_link.id,  max(movie_link.movie_id) from movie_link group by movie_link.id;
select movie_link.id,  sum(movie_link.link_type_id) from movie_link group by movie_link.id;
select movie_link.id,  avg(movie_link.id) from movie_link group by movie_link.id;
select movie_link.linked_movie_id, link_type.id,  min(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select movie_link.movie_id, link_type.id,  max(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select movie_link.linked_movie_id, link_type.id,  sum(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.linked_movie_id, link_type.id;
select movie_link.id,  avg(movie_link.id) from movie_link group by movie_link.id;
select movie_link.movie_id,  sum(movie_link.link_type_id) from movie_link group by movie_link.movie_id order by movie_link.movie_id ASC;
select movie_link.id, link_type.id,  max(movie_link.link_type_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.id, link_type.id;
select movie_link.id,  sum(movie_link.id) from movie_link group by movie_link.id having sum(movie_link.id) <= 1726;
select link_type.id, movie_link.id,  max(movie_link.link_type_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by link_type.id, movie_link.id;
select movie_link.id, link_type.id,  min(movie_link.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.id, link_type.id;
select link_type.id, movie_link.movie_id,  min(movie_link.link_type_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by link_type.id, movie_link.movie_id;
select movie_link.id,  sum(movie_link.link_type_id) from movie_link group by movie_link.id;
select movie_link.movie_id,  max(movie_link.id) from movie_link group by movie_link.movie_id order by movie_link.movie_id DESC;
select movie_link.linked_movie_id,  avg(movie_link.link_type_id) from movie_link group by movie_link.linked_movie_id having avg(movie_link.link_type_id) >= 294;
select movie_link.movie_id,  min(movie_link.movie_id) from movie_link group by movie_link.movie_id order by movie_link.movie_id DESC;
select movie_link.movie_id, link_type.id,  sum(movie_link.link_type_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select movie_link.id, link_type.id,  min(movie_link.link_type_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.id, link_type.id;
select movie_link.id,  min(movie_link.linked_movie_id) from movie_link group by movie_link.id;
select movie_link.id,  min(movie_link.link_type_id) from movie_link group by movie_link.id;
select movie_link.movie_id, link_type.id,  sum(movie_link.link_type_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select movie_link.movie_id, link_type.id,  sum(movie_link.linked_movie_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select movie_link.linked_movie_id, link_type.id,  min(movie_link.link_type_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.linked_movie_id, link_type.id;
select movie_link.id,  sum(movie_link.id) from movie_link group by movie_link.id having sum(movie_link.id) < 1727;
select link_type.id, movie_link.movie_id,  sum(movie_link.movie_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by link_type.id, movie_link.movie_id;
select movie_link.id, link_type.id,  sum(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.id, link_type.id;
select movie_link.id,  sum(movie_link.movie_id) from movie_link group by movie_link.id;
select movie_link.movie_id,  sum(movie_link.linked_movie_id) from movie_link group by movie_link.movie_id order by movie_link.movie_id DESC;
select movie_link.movie_id, link_type.id,  max(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select movie_link.id, link_type.id,  sum(movie_link.link_type_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.id, link_type.id;
select movie_link.linked_movie_id,  avg(movie_link.linked_movie_id) from movie_link group by movie_link.linked_movie_id having avg(movie_link.linked_movie_id) != 1905;
select movie_link.linked_movie_id, link_type.id,  max(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select movie_link.id,  sum(movie_link.linked_movie_id) from movie_link group by movie_link.id having sum(movie_link.linked_movie_id) >= 1889;
select movie_link.linked_movie_id, link_type.id,  min(movie_link.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select movie_link.movie_id, link_type.id,  sum(movie_link.movie_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select link_type.id, movie_link.linked_movie_id,  sum(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by link_type.id, movie_link.linked_movie_id;
select movie_link.movie_id, link_type.id,  min(movie_link.movie_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select movie_link.id, link_type.id,  sum(movie_link.link_type_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.id, link_type.id;
select movie_link.movie_id,  min(movie_link.id) from movie_link group by movie_link.movie_id order by movie_link.movie_id DESC;
select movie_link.movie_id, link_type.id,  sum(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.movie_id, link_type.id;
select movie_link.id,  max(movie_link.id) from movie_link group by movie_link.id;
select movie_link.id, link_type.id,  sum(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.id, link_type.id;
select movie_link.id, link_type.id,  sum(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.id, link_type.id;
select movie_link.id,  max(movie_link.link_type_id) from movie_link group by movie_link.id;
select movie_link.id,  sum(movie_link.id) from movie_link group by movie_link.id having sum(movie_link.id) != 1683;
select movie_link.movie_id, link_type.id,  min(movie_link.linked_movie_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.movie_id, link_type.id;
select movie_link.linked_movie_id, link_type.id,  max(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select movie_link.linked_movie_id, link_type.id,  sum(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.linked_movie_id, link_type.id;
select movie_link.movie_id,  max(movie_link.link_type_id) from movie_link group by movie_link.movie_id order by movie_link.movie_id DESC;
select link_type.id, movie_link.id,  max(movie_link.linked_movie_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by link_type.id, movie_link.id;
select movie_link.id,  max(movie_link.link_type_id) from movie_link group by movie_link.id;
select movie_link.linked_movie_id, link_type.id,  sum(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select movie_link.linked_movie_id, link_type.id,  max(movie_link.linked_movie_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.linked_movie_id, link_type.id;
select movie_link.id,  sum(movie_link.linked_movie_id) from movie_link group by movie_link.id;
select movie_link.linked_movie_id, link_type.id,  max(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select movie_link.id, link_type.id,  max(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.id, link_type.id;
select movie_link.id, link_type.id,  min(movie_link.movie_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.id, link_type.id;
select movie_link.id,  sum(movie_link.linked_movie_id) from movie_link group by movie_link.id;
select movie_link.movie_id, link_type.id,  min(movie_link.movie_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.movie_id, link_type.id;
select movie_link.movie_id,  max(movie_link.id) from movie_link group by movie_link.movie_id order by movie_link.movie_id ASC;
select movie_link.id, link_type.id,  sum(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.id, link_type.id;
select movie_link.id,  avg(movie_link.linked_movie_id) from movie_link group by movie_link.id;
select movie_link.movie_id,  sum(movie_link.id) from movie_link group by movie_link.movie_id order by movie_link.movie_id ASC;
select movie_link.id, link_type.id,  min(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.id, link_type.id;
select movie_link.id, link_type.id,  max(movie_link.movie_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.id, link_type.id;
select link_type.id, movie_link.id,  sum(movie_link.linked_movie_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by link_type.id, movie_link.id;
select movie_link.id, link_type.id,  min(movie_link.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.id, link_type.id;
select link_type.id, movie_link.movie_id,  sum(movie_link.link_type_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by link_type.id, movie_link.movie_id;
select movie_link.linked_movie_id, link_type.id,  min(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select movie_link.id, link_type.id,  min(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.id, link_type.id;
select movie_link.linked_movie_id, link_type.id,  sum(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select movie_link.id, link_type.id,  min(movie_link.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.id, link_type.id;
select movie_link.id, link_type.id,  min(movie_link.linked_movie_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.id, link_type.id;
select movie_link.id, link_type.id,  sum(movie_link.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.id, link_type.id;
select movie_link.linked_movie_id, link_type.id,  max(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.linked_movie_id, link_type.id;
select movie_link.linked_movie_id, link_type.id,  max(movie_link.link_type_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select movie_link.movie_id,  max(movie_link.movie_id) from movie_link group by movie_link.movie_id order by movie_link.movie_id ASC;
select movie_link.movie_id, link_type.id,  max(movie_link.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.movie_id, link_type.id;
select movie_link.movie_id,  max(movie_link.linked_movie_id) from movie_link group by movie_link.movie_id order by movie_link.movie_id DESC;
select movie_link.id, link_type.id,  max(movie_link.linked_movie_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.id, link_type.id;
select movie_link.linked_movie_id,  avg(movie_link.id) from movie_link group by movie_link.linked_movie_id having avg(movie_link.id) != 1668;
select link_type.id, movie_link.linked_movie_id,  min(movie_link.linked_movie_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by link_type.id, movie_link.linked_movie_id;
select movie_link.id,  sum(movie_link.id) from movie_link group by movie_link.id having sum(movie_link.id) >= 1744;
select movie_link.movie_id, link_type.id,  sum(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.movie_id, link_type.id;
select movie_link.movie_id,  max(movie_link.link_type_id) from movie_link group by movie_link.movie_id order by movie_link.movie_id DESC;
select movie_link.movie_id, link_type.id,  sum(movie_link.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.movie_id, link_type.id;
select movie_link.id, link_type.id,  min(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.id, link_type.id;
select movie_link.id,  max(movie_link.link_type_id) from movie_link group by movie_link.id;
select movie_link.movie_id, link_type.id,  min(movie_link.linked_movie_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.movie_id, link_type.id;
select movie_link.id, link_type.id,  sum(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.id, link_type.id;
select link_type.id, movie_link.movie_id,  min(movie_link.linked_movie_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by link_type.id, movie_link.movie_id;
select movie_link.id,  min(movie_link.link_type_id) from movie_link group by movie_link.id;
select movie_link.movie_id, link_type.id,  min(movie_link.movie_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select movie_link.movie_id, link_type.id,  sum(movie_link.link_type_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.movie_id, link_type.id;
select movie_link.movie_id, link_type.id,  sum(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select movie_link.id,  avg(movie_link.id) from movie_link group by movie_link.id;
select link_type.id, movie_link.id,  sum(movie_link.movie_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by link_type.id, movie_link.id;
select movie_link.id, link_type.id,  max(movie_link.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.id, link_type.id;
select movie_link.movie_id, link_type.id,  max(movie_link.linked_movie_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.movie_id, link_type.id;
select link_type.id, movie_link.movie_id,  min(movie_link.linked_movie_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by link_type.id, movie_link.movie_id;
select link_type.id, movie_link.linked_movie_id,  min(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by link_type.id, movie_link.linked_movie_id;
select movie_link.movie_id, link_type.id,  max(movie_link.movie_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select movie_link.linked_movie_id, link_type.id,  min(movie_link.linked_movie_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select movie_link.id, link_type.id,  min(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.id, link_type.id;
select movie_link.movie_id, link_type.id,  sum(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select movie_link.movie_id, link_type.id,  sum(movie_link.movie_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.movie_id, link_type.id;
select movie_link.linked_movie_id, link_type.id,  min(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.linked_movie_id, link_type.id;
select movie_link.id, link_type.id,  min(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.id, link_type.id;
select movie_link.id,  min(movie_link.link_type_id) from movie_link group by movie_link.id;
select movie_link.id,  sum(movie_link.movie_id) from movie_link group by movie_link.id;
select movie_link.id,  min(movie_link.id) from movie_link group by movie_link.id;
select link_type.id, movie_link.movie_id,  sum(movie_link.linked_movie_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by link_type.id, movie_link.movie_id;
select link_type.id, movie_link.movie_id,  sum(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by link_type.id, movie_link.movie_id;
select movie_link.linked_movie_id, link_type.id,  sum(movie_link.linked_movie_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.linked_movie_id, link_type.id;
select movie_link.movie_id, link_type.id,  sum(movie_link.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select movie_link.linked_movie_id, link_type.id,  min(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.linked_movie_id, link_type.id;
select movie_link.linked_movie_id, link_type.id,  max(movie_link.movie_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select movie_link.linked_movie_id, link_type.id,  max(movie_link.movie_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.linked_movie_id, link_type.id;
select movie_link.id,  max(movie_link.linked_movie_id) from movie_link group by movie_link.id;
select movie_link.id, link_type.id,  sum(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.id, link_type.id;
select movie_link.movie_id,  min(movie_link.link_type_id) from movie_link group by movie_link.movie_id order by movie_link.movie_id ASC;
select movie_link.id,  avg(movie_link.movie_id) from movie_link group by movie_link.id;
select movie_link.id, link_type.id,  sum(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.id, link_type.id;
select movie_link.linked_movie_id, link_type.id,  sum(movie_link.linked_movie_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.linked_movie_id, link_type.id;
select movie_link.movie_id, link_type.id,  sum(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select movie_link.id, link_type.id,  min(movie_link.linked_movie_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.id, link_type.id;
select movie_link.linked_movie_id, link_type.id,  max(movie_link.movie_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select movie_link.id,  min(movie_link.movie_id) from movie_link group by movie_link.id having min(movie_link.movie_id) != 1796;
select movie_link.linked_movie_id, link_type.id,  max(movie_link.movie_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select link_type.id, movie_link.linked_movie_id,  sum(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by link_type.id, movie_link.linked_movie_id;
select movie_link.id,  max(movie_link.movie_id) from movie_link group by movie_link.id;
select movie_link.id,  sum(movie_link.id) from movie_link group by movie_link.id having sum(movie_link.id) != 1711;
select movie_link.id,  min(movie_link.linked_movie_id) from movie_link group by movie_link.id having min(movie_link.linked_movie_id) < 1895;
select movie_link.movie_id, link_type.id,  min(movie_link.linked_movie_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select movie_link.linked_movie_id,  avg(movie_link.id) from movie_link group by movie_link.linked_movie_id having avg(movie_link.id) <= 1673;
select movie_link.id, link_type.id,  sum(movie_link.movie_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.id, link_type.id;
select movie_link.id, link_type.id,  max(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.id, link_type.id;
select movie_link.id,  avg(movie_link.id) from movie_link group by movie_link.id;
select movie_link.id,  avg(movie_link.movie_id) from movie_link group by movie_link.id;
select movie_link.linked_movie_id, link_type.id,  max(movie_link.movie_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select movie_link.id, link_type.id,  sum(movie_link.linked_movie_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.id, link_type.id;
select link_type.id, movie_link.linked_movie_id,  sum(movie_link.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by link_type.id, movie_link.linked_movie_id;
select movie_link.id,  sum(movie_link.movie_id) from movie_link group by movie_link.id;
select movie_link.linked_movie_id, link_type.id,  min(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select movie_link.movie_id, link_type.id,  sum(movie_link.linked_movie_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select movie_link.id,  avg(movie_link.movie_id) from movie_link group by movie_link.id;
select movie_link.id,  sum(movie_link.id) from movie_link group by movie_link.id;
select movie_link.id,  max(movie_link.id) from movie_link group by movie_link.id;
select movie_link.id,  max(movie_link.linked_movie_id) from movie_link group by movie_link.id;
select movie_link.id,  min(movie_link.link_type_id) from movie_link group by movie_link.id having min(movie_link.link_type_id) > 179;
select movie_link.movie_id,  sum(movie_link.movie_id) from movie_link group by movie_link.movie_id order by movie_link.movie_id ASC;
select movie_link.id,  min(movie_link.id) from movie_link group by movie_link.id having min(movie_link.id) <= 1754;
select movie_link.id,  sum(movie_link.link_type_id) from movie_link group by movie_link.id;
select movie_link.movie_id, link_type.id,  min(movie_link.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select movie_link.movie_id, link_type.id,  sum(movie_link.movie_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.movie_id, link_type.id;
select movie_link.linked_movie_id, link_type.id,  min(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.linked_movie_id, link_type.id;
select link_type.id, movie_link.id,  max(movie_link.link_type_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by link_type.id, movie_link.id;
select movie_link.linked_movie_id, link_type.id,  sum(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.linked_movie_id, link_type.id;
select movie_link.linked_movie_id, link_type.id,  sum(movie_link.linked_movie_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select movie_link.id,  sum(movie_link.link_type_id) from movie_link group by movie_link.id having sum(movie_link.link_type_id) < 287;
select link_type.id, movie_link.linked_movie_id,  min(movie_link.linked_movie_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by link_type.id, movie_link.linked_movie_id;
select movie_link.movie_id,  min(movie_link.linked_movie_id) from movie_link group by movie_link.movie_id order by movie_link.movie_id DESC;
select movie_link.movie_id,  max(movie_link.id) from movie_link group by movie_link.movie_id order by movie_link.movie_id ASC;
select link_type.id, movie_link.linked_movie_id,  min(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by link_type.id, movie_link.linked_movie_id;
select movie_link.movie_id,  max(movie_link.id) from movie_link group by movie_link.movie_id order by movie_link.movie_id ASC;
select movie_link.id,  sum(movie_link.link_type_id) from movie_link group by movie_link.id having sum(movie_link.link_type_id) > 288;
select movie_link.movie_id, link_type.id,  min(movie_link.linked_movie_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.movie_id, link_type.id;
select movie_link.movie_id, link_type.id,  sum(movie_link.movie_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select link_type.id, movie_link.id,  max(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by link_type.id, movie_link.id;
select link_type.id, movie_link.linked_movie_id,  min(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by link_type.id, movie_link.linked_movie_id;
select movie_link.id,  max(movie_link.id) from movie_link group by movie_link.id;
select link_type.id, movie_link.id,  min(movie_link.link_type_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by link_type.id, movie_link.id;
select movie_link.movie_id,  sum(movie_link.movie_id) from movie_link group by movie_link.movie_id order by movie_link.movie_id DESC;
select movie_link.linked_movie_id, link_type.id,  sum(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.linked_movie_id, link_type.id;
select movie_link.movie_id, link_type.id,  sum(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.movie_id, link_type.id;
select movie_link.id, link_type.id,  max(movie_link.link_type_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.id, link_type.id;
select movie_link.linked_movie_id, link_type.id,  min(movie_link.linked_movie_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.linked_movie_id, link_type.id;
select link_type.id, movie_link.id,  min(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by link_type.id, movie_link.id;
select movie_link.id,  avg(movie_link.id) from movie_link group by movie_link.id;
select movie_link.linked_movie_id, link_type.id,  max(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.linked_movie_id, link_type.id;
select movie_link.linked_movie_id,  avg(movie_link.link_type_id) from movie_link group by movie_link.linked_movie_id having avg(movie_link.link_type_id) < 286;
select movie_link.linked_movie_id, link_type.id,  sum(movie_link.linked_movie_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.linked_movie_id, link_type.id;
select link_type.id, movie_link.linked_movie_id,  min(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by link_type.id, movie_link.linked_movie_id;
select movie_link.linked_movie_id, link_type.id,  min(movie_link.link_type_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.linked_movie_id, link_type.id;
select movie_link.id,  sum(movie_link.id) from movie_link group by movie_link.id;
select movie_link.linked_movie_id, link_type.id,  max(movie_link.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select movie_link.movie_id, link_type.id,  sum(movie_link.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select movie_link.movie_id,  max(movie_link.link_type_id) from movie_link group by movie_link.movie_id order by movie_link.movie_id DESC;
select movie_link.id,  min(movie_link.linked_movie_id) from movie_link group by movie_link.id;
select movie_link.movie_id, link_type.id,  min(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select movie_link.id, link_type.id,  max(movie_link.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.id, link_type.id;
select movie_link.movie_id, link_type.id,  min(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select movie_link.id,  sum(movie_link.movie_id) from movie_link group by movie_link.id;
select movie_link.movie_id, link_type.id,  max(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.movie_id, link_type.id;
select movie_link.id, link_type.id,  max(movie_link.linked_movie_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.id, link_type.id;
select movie_link.movie_id, link_type.id,  max(movie_link.link_type_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.movie_id, link_type.id;
select movie_link.linked_movie_id, link_type.id,  sum(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.linked_movie_id, link_type.id;
select movie_link.id, link_type.id,  max(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.id, link_type.id;
select movie_link.id, link_type.id,  sum(movie_link.movie_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.id, link_type.id;
select movie_link.id, link_type.id,  sum(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.id, link_type.id;
select movie_link.id,  min(movie_link.linked_movie_id) from movie_link group by movie_link.id;
select movie_link.movie_id,  max(movie_link.id) from movie_link group by movie_link.movie_id order by movie_link.movie_id ASC;
select movie_link.id,  avg(movie_link.linked_movie_id) from movie_link group by movie_link.id;
select movie_link.id,  sum(movie_link.movie_id) from movie_link group by movie_link.id having sum(movie_link.movie_id) < 1819;
select movie_link.id,  sum(movie_link.linked_movie_id) from movie_link group by movie_link.id having sum(movie_link.linked_movie_id) < 1864;
select movie_link.id, link_type.id,  sum(movie_link.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.id, link_type.id;
select movie_link.id,  max(movie_link.link_type_id) from movie_link group by movie_link.id having max(movie_link.link_type_id) <= 282;
select movie_link.id, link_type.id,  max(movie_link.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.id, link_type.id;
select movie_link.id,  min(movie_link.link_type_id) from movie_link group by movie_link.id;
select movie_link.id,  max(movie_link.link_type_id) from movie_link group by movie_link.id;
select movie_link.linked_movie_id, link_type.id,  max(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select movie_link.movie_id,  sum(movie_link.linked_movie_id) from movie_link group by movie_link.movie_id order by movie_link.movie_id DESC;
select movie_link.movie_id, link_type.id,  max(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select movie_link.linked_movie_id, link_type.id,  min(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select movie_link.id,  avg(movie_link.movie_id) from movie_link group by movie_link.id;
select movie_link.linked_movie_id,  avg(movie_link.id) from movie_link group by movie_link.linked_movie_id having avg(movie_link.id) = 1703;
select movie_link.movie_id,  max(movie_link.movie_id) from movie_link group by movie_link.movie_id order by movie_link.movie_id ASC;
select movie_link.id,  max(movie_link.link_type_id) from movie_link group by movie_link.id;
select movie_link.movie_id,  sum(movie_link.id) from movie_link group by movie_link.movie_id order by movie_link.movie_id DESC;
select movie_link.id, link_type.id,  sum(movie_link.link_type_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.id, link_type.id;
select movie_link.id,  min(movie_link.linked_movie_id) from movie_link group by movie_link.id;
select movie_link.movie_id, link_type.id,  max(movie_link.linked_movie_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select movie_link.movie_id,  max(movie_link.id) from movie_link group by movie_link.movie_id order by movie_link.movie_id DESC;
select movie_link.movie_id, link_type.id,  sum(movie_link.linked_movie_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select movie_link.id,  avg(movie_link.id) from movie_link group by movie_link.id;
select link_type.id, movie_link.id,  sum(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by link_type.id, movie_link.id;
select movie_link.linked_movie_id, link_type.id,  min(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select movie_link.linked_movie_id, link_type.id,  min(movie_link.movie_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.linked_movie_id, link_type.id;
select movie_link.movie_id, link_type.id,  max(movie_link.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.movie_id, link_type.id;
select movie_link.movie_id, link_type.id,  sum(movie_link.link_type_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select movie_link.id,  min(movie_link.link_type_id) from movie_link group by movie_link.id;
select link_type.id, movie_link.id,  min(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by link_type.id, movie_link.id;
select movie_link.id,  min(movie_link.id) from movie_link group by movie_link.id;
select movie_link.id,  sum(movie_link.link_type_id) from movie_link group by movie_link.id having sum(movie_link.link_type_id) < 179;
select movie_link.id,  sum(movie_link.linked_movie_id) from movie_link group by movie_link.id having sum(movie_link.linked_movie_id) > 1892;
select movie_link.linked_movie_id, link_type.id,  min(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select movie_link.movie_id,  sum(movie_link.id) from movie_link group by movie_link.movie_id order by movie_link.movie_id ASC;
select movie_link.movie_id,  min(movie_link.link_type_id) from movie_link group by movie_link.movie_id order by movie_link.movie_id DESC;
select movie_link.movie_id, link_type.id,  min(movie_link.movie_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select movie_link.id,  max(movie_link.linked_movie_id) from movie_link group by movie_link.id;
select movie_link.id,  max(movie_link.linked_movie_id) from movie_link group by movie_link.id;
select movie_link.movie_id,  min(movie_link.movie_id) from movie_link group by movie_link.movie_id order by movie_link.movie_id ASC;
select movie_link.movie_id, link_type.id,  sum(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select movie_link.id,  max(movie_link.movie_id) from movie_link group by movie_link.id;
select movie_link.id, link_type.id,  max(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.id, link_type.id;
select movie_link.movie_id,  sum(movie_link.movie_id) from movie_link group by movie_link.movie_id order by movie_link.movie_id DESC;
select movie_link.linked_movie_id, link_type.id,  sum(movie_link.linked_movie_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.linked_movie_id, link_type.id;
select movie_link.linked_movie_id, link_type.id,  max(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select movie_link.id,  sum(movie_link.movie_id) from movie_link group by movie_link.id;
select movie_link.movie_id, link_type.id,  min(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select movie_link.id,  avg(movie_link.link_type_id) from movie_link group by movie_link.id;
select movie_link.id,  min(movie_link.link_type_id) from movie_link group by movie_link.id having min(movie_link.link_type_id) <= 289;
select movie_link.linked_movie_id, link_type.id,  min(movie_link.link_type_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select movie_link.movie_id, link_type.id,  sum(movie_link.link_type_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.movie_id, link_type.id;
select movie_link.id,  max(movie_link.id) from movie_link group by movie_link.id;
select movie_link.id,  min(movie_link.linked_movie_id) from movie_link group by movie_link.id having min(movie_link.linked_movie_id) != 1881;
select movie_link.movie_id,  sum(movie_link.linked_movie_id) from movie_link group by movie_link.movie_id order by movie_link.movie_id ASC;
select link_type.id, movie_link.id,  sum(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by link_type.id, movie_link.id;
select movie_link.movie_id,  max(movie_link.movie_id) from movie_link group by movie_link.movie_id order by movie_link.movie_id DESC;
select movie_link.linked_movie_id, link_type.id,  min(movie_link.movie_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select movie_link.linked_movie_id, link_type.id,  min(movie_link.linked_movie_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select movie_link.id, link_type.id,  sum(movie_link.linked_movie_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.id, link_type.id;
select movie_link.movie_id, link_type.id,  min(movie_link.linked_movie_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select movie_link.id,  min(movie_link.link_type_id) from movie_link group by movie_link.id having min(movie_link.link_type_id) < 286;
select movie_link.id,  avg(movie_link.id) from movie_link group by movie_link.id;
select movie_link.linked_movie_id, link_type.id,  min(movie_link.movie_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select movie_link.movie_id,  sum(movie_link.link_type_id) from movie_link group by movie_link.movie_id order by movie_link.movie_id DESC;
select movie_link.movie_id,  sum(movie_link.linked_movie_id) from movie_link group by movie_link.movie_id order by movie_link.movie_id DESC;
select movie_link.id,  sum(movie_link.id) from movie_link group by movie_link.id;
select movie_link.linked_movie_id,  avg(movie_link.movie_id) from movie_link group by movie_link.linked_movie_id having avg(movie_link.movie_id) > 1797;
select movie_link.linked_movie_id,  avg(movie_link.id) from movie_link group by movie_link.linked_movie_id having avg(movie_link.id) < 1692;
select movie_link.id,  avg(movie_link.id) from movie_link group by movie_link.id;
select movie_link.movie_id, link_type.id,  sum(movie_link.linked_movie_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select link_type.id, movie_link.movie_id,  max(movie_link.link_type_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by link_type.id, movie_link.movie_id;
select movie_link.movie_id,  min(movie_link.linked_movie_id) from movie_link group by movie_link.movie_id order by movie_link.movie_id ASC;
select movie_link.movie_id,  min(movie_link.linked_movie_id) from movie_link group by movie_link.movie_id order by movie_link.movie_id ASC;
select movie_link.linked_movie_id, link_type.id,  max(movie_link.link_type_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.linked_movie_id, link_type.id;
select movie_link.movie_id, link_type.id,  sum(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.movie_id, link_type.id;
select movie_link.id,  max(movie_link.id) from movie_link group by movie_link.id having max(movie_link.id) != 1753;
select movie_link.id,  min(movie_link.linked_movie_id) from movie_link group by movie_link.id;
select movie_link.id,  sum(movie_link.movie_id) from movie_link group by movie_link.id having sum(movie_link.movie_id) > 1814;
select movie_link.movie_id, link_type.id,  sum(movie_link.link_type_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select movie_link.id,  avg(movie_link.id) from movie_link group by movie_link.id;
select movie_link.id, link_type.id,  sum(movie_link.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.id, link_type.id;
select link_type.id, movie_link.linked_movie_id,  sum(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by link_type.id, movie_link.linked_movie_id;
select movie_link.id, link_type.id,  sum(movie_link.movie_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.id, link_type.id;
select movie_link.id,  sum(movie_link.id) from movie_link group by movie_link.id;
select movie_link.id,  sum(movie_link.link_type_id) from movie_link group by movie_link.id having sum(movie_link.link_type_id) = 290;
select movie_link.movie_id,  min(movie_link.linked_movie_id) from movie_link group by movie_link.movie_id order by movie_link.movie_id DESC;
select link_type.id, movie_link.movie_id,  max(movie_link.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by link_type.id, movie_link.movie_id;
select movie_link.movie_id,  sum(movie_link.movie_id) from movie_link group by movie_link.movie_id order by movie_link.movie_id DESC;
select movie_link.linked_movie_id, link_type.id,  max(movie_link.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.linked_movie_id, link_type.id;
select movie_link.linked_movie_id, link_type.id,  sum(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select movie_link.id,  sum(movie_link.linked_movie_id) from movie_link group by movie_link.id;
select link_type.id, movie_link.linked_movie_id,  min(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by link_type.id, movie_link.linked_movie_id;
select movie_link.movie_id, link_type.id,  min(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select movie_link.id, link_type.id,  sum(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.id, link_type.id;
select movie_link.id, link_type.id,  sum(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.id, link_type.id;
select movie_link.movie_id,  min(movie_link.movie_id) from movie_link group by movie_link.movie_id order by movie_link.movie_id DESC;
select link_type.id, movie_link.movie_id,  sum(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by link_type.id, movie_link.movie_id;
select movie_link.movie_id,  min(movie_link.linked_movie_id) from movie_link group by movie_link.movie_id order by movie_link.movie_id DESC;
select movie_link.id,  min(movie_link.linked_movie_id) from movie_link group by movie_link.id having min(movie_link.linked_movie_id) <= 1919;
select link_type.id, movie_link.linked_movie_id,  sum(movie_link.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by link_type.id, movie_link.linked_movie_id;
select movie_link.linked_movie_id, link_type.id,  max(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.linked_movie_id, link_type.id;
select movie_link.id, link_type.id,  sum(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.id, link_type.id;
select movie_link.id, link_type.id,  min(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.id, link_type.id;
select movie_link.linked_movie_id, link_type.id,  min(movie_link.movie_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.linked_movie_id, link_type.id;
select movie_link.movie_id,  min(movie_link.movie_id) from movie_link group by movie_link.movie_id order by movie_link.movie_id DESC;
select movie_link.movie_id,  max(movie_link.id) from movie_link group by movie_link.movie_id order by movie_link.movie_id ASC;
select movie_link.id, link_type.id,  min(movie_link.link_type_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.id, link_type.id;
select movie_link.id,  min(movie_link.linked_movie_id) from movie_link group by movie_link.id having min(movie_link.linked_movie_id) < 1898;
select movie_link.id, link_type.id,  min(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.id, link_type.id;
select movie_link.movie_id,  sum(movie_link.link_type_id) from movie_link group by movie_link.movie_id order by movie_link.movie_id DESC;
select movie_link.id, link_type.id,  sum(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.id, link_type.id;
select movie_link.movie_id,  min(movie_link.movie_id) from movie_link group by movie_link.movie_id order by movie_link.movie_id DESC;
select movie_link.id,  avg(movie_link.linked_movie_id) from movie_link group by movie_link.id;
select link_type.id, movie_link.movie_id,  min(movie_link.movie_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by link_type.id, movie_link.movie_id;
select movie_link.id, link_type.id,  sum(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.id, link_type.id;
select movie_link.movie_id, link_type.id,  max(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select movie_link.linked_movie_id, link_type.id,  max(movie_link.linked_movie_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.linked_movie_id, link_type.id;
select movie_link.movie_id,  max(movie_link.movie_id) from movie_link group by movie_link.movie_id order by movie_link.movie_id ASC;
select movie_link.movie_id, link_type.id,  sum(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.movie_id, link_type.id;
select movie_link.movie_id, link_type.id,  min(movie_link.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select movie_link.movie_id, link_type.id,  sum(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select movie_link.movie_id, link_type.id,  sum(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select movie_link.id,  sum(movie_link.linked_movie_id) from movie_link group by movie_link.id having sum(movie_link.linked_movie_id) != 1921;
select movie_link.movie_id,  max(movie_link.link_type_id) from movie_link group by movie_link.movie_id order by movie_link.movie_id ASC;
select movie_link.id,  max(movie_link.link_type_id) from movie_link group by movie_link.id having max(movie_link.link_type_id) <= 178;
select link_type.id, movie_link.id,  max(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by link_type.id, movie_link.id;
select movie_link.movie_id, link_type.id,  sum(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select movie_link.id, link_type.id,  sum(movie_link.movie_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.id, link_type.id;
select movie_link.id,  max(movie_link.link_type_id) from movie_link group by movie_link.id;
select movie_link.linked_movie_id, link_type.id,  max(movie_link.link_type_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select movie_link.linked_movie_id, link_type.id,  sum(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select link_type.id, movie_link.id,  max(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by link_type.id, movie_link.id;
select link_type.id, movie_link.movie_id,  min(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by link_type.id, movie_link.movie_id;
select movie_link.id,  min(movie_link.linked_movie_id) from movie_link group by movie_link.id;
select movie_link.movie_id,  sum(movie_link.linked_movie_id) from movie_link group by movie_link.movie_id order by movie_link.movie_id DESC;
select link_type.id, movie_link.id,  max(movie_link.movie_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by link_type.id, movie_link.id;
select link_type.id, movie_link.movie_id,  max(movie_link.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by link_type.id, movie_link.movie_id;
select link_type.id, movie_link.linked_movie_id,  max(movie_link.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by link_type.id, movie_link.linked_movie_id;
select movie_link.id,  max(movie_link.movie_id) from movie_link group by movie_link.id having max(movie_link.movie_id) = 1790;
select movie_link.linked_movie_id, link_type.id,  max(movie_link.linked_movie_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select movie_link.movie_id, link_type.id,  sum(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.movie_id, link_type.id;
select movie_link.movie_id,  max(movie_link.linked_movie_id) from movie_link group by movie_link.movie_id order by movie_link.movie_id DESC;
select movie_link.movie_id, link_type.id,  min(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select movie_link.movie_id,  max(movie_link.movie_id) from movie_link group by movie_link.movie_id order by movie_link.movie_id ASC;
select movie_link.linked_movie_id, link_type.id,  min(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select movie_link.movie_id, link_type.id,  sum(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.movie_id, link_type.id;
select movie_link.id,  avg(movie_link.link_type_id) from movie_link group by movie_link.id;
select movie_link.movie_id, link_type.id,  max(movie_link.movie_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select movie_link.id, link_type.id,  min(movie_link.movie_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.id, link_type.id;
select movie_link.id, link_type.id,  sum(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.id, link_type.id;
select movie_link.movie_id, link_type.id,  max(movie_link.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select movie_link.id,  sum(movie_link.link_type_id) from movie_link group by movie_link.id;
select link_type.id, movie_link.linked_movie_id,  max(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by link_type.id, movie_link.linked_movie_id;
select movie_link.id,  min(movie_link.id) from movie_link group by movie_link.id;
select link_type.id, movie_link.id,  min(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by link_type.id, movie_link.id;
select movie_link.id,  max(movie_link.id) from movie_link group by movie_link.id;
select link_type.id, movie_link.movie_id,  max(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by link_type.id, movie_link.movie_id;
select link_type.id, movie_link.movie_id,  max(movie_link.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by link_type.id, movie_link.movie_id;
select movie_link.id, link_type.id,  sum(movie_link.linked_movie_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.id, link_type.id;
select movie_link.movie_id,  sum(movie_link.link_type_id) from movie_link group by movie_link.movie_id order by movie_link.movie_id ASC;
select movie_link.linked_movie_id, link_type.id,  min(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select movie_link.movie_id,  min(movie_link.id) from movie_link group by movie_link.movie_id order by movie_link.movie_id ASC;
select movie_link.movie_id,  max(movie_link.link_type_id) from movie_link group by movie_link.movie_id order by movie_link.movie_id DESC;
select link_type.id, movie_link.linked_movie_id,  min(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by link_type.id, movie_link.linked_movie_id;
select movie_link.linked_movie_id, link_type.id,  min(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.linked_movie_id, link_type.id;
select movie_link.id, link_type.id,  min(movie_link.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.id, link_type.id;
select movie_link.linked_movie_id, link_type.id,  sum(movie_link.linked_movie_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.linked_movie_id, link_type.id;
select movie_link.linked_movie_id, link_type.id,  sum(movie_link.link_type_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.linked_movie_id, link_type.id;
select movie_link.id,  avg(movie_link.movie_id) from movie_link group by movie_link.id;
select movie_link.id,  avg(movie_link.linked_movie_id) from movie_link group by movie_link.id;
select link_type.id, movie_link.id,  max(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by link_type.id, movie_link.id;
select movie_link.id,  min(movie_link.link_type_id) from movie_link group by movie_link.id;
select movie_link.id, link_type.id,  max(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.id, link_type.id;
select link_type.id, movie_link.movie_id,  sum(movie_link.linked_movie_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by link_type.id, movie_link.movie_id;
select movie_link.linked_movie_id,  avg(movie_link.link_type_id) from movie_link group by movie_link.linked_movie_id having avg(movie_link.link_type_id) != 181;
select movie_link.movie_id, link_type.id,  min(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.movie_id, link_type.id;
select movie_link.linked_movie_id, link_type.id,  sum(movie_link.link_type_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.linked_movie_id, link_type.id;
select link_type.id, movie_link.linked_movie_id,  min(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by link_type.id, movie_link.linked_movie_id;
select movie_link.id, link_type.id,  sum(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.id, link_type.id;
select movie_link.id,  max(movie_link.linked_movie_id) from movie_link group by movie_link.id;
select movie_link.movie_id, link_type.id,  min(movie_link.linked_movie_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select movie_link.linked_movie_id, link_type.id,  min(movie_link.movie_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.linked_movie_id, link_type.id;
select movie_link.id, link_type.id,  max(movie_link.linked_movie_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.id, link_type.id;
select movie_link.movie_id,  sum(movie_link.linked_movie_id) from movie_link group by movie_link.movie_id order by movie_link.movie_id ASC;
select movie_link.movie_id, link_type.id,  sum(movie_link.linked_movie_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select movie_link.movie_id,  max(movie_link.id) from movie_link group by movie_link.movie_id order by movie_link.movie_id DESC;
select movie_link.id,  max(movie_link.movie_id) from movie_link group by movie_link.id having max(movie_link.movie_id) != 1767;
select movie_link.id,  sum(movie_link.id) from movie_link group by movie_link.id having sum(movie_link.id) = 1740;
select movie_link.id,  sum(movie_link.link_type_id) from movie_link group by movie_link.id;
select movie_link.id, link_type.id,  sum(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.id, link_type.id;
select movie_link.linked_movie_id, link_type.id,  max(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select link_type.id, movie_link.linked_movie_id,  max(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by link_type.id, movie_link.linked_movie_id;
select link_type.id, movie_link.linked_movie_id,  max(movie_link.linked_movie_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by link_type.id, movie_link.linked_movie_id;
select movie_link.movie_id,  max(movie_link.movie_id) from movie_link group by movie_link.movie_id order by movie_link.movie_id DESC;
select link_type.id, movie_link.linked_movie_id,  max(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by link_type.id, movie_link.linked_movie_id;
select movie_link.id,  sum(movie_link.movie_id) from movie_link group by movie_link.id having sum(movie_link.movie_id) > 1809;
select movie_link.movie_id, link_type.id,  sum(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select movie_link.id,  sum(movie_link.movie_id) from movie_link group by movie_link.id having sum(movie_link.movie_id) >= 1786;
select movie_link.linked_movie_id, link_type.id,  sum(movie_link.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select movie_link.linked_movie_id, link_type.id,  sum(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select movie_link.linked_movie_id, link_type.id,  max(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.linked_movie_id, link_type.id;
select movie_link.id, link_type.id,  max(movie_link.linked_movie_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.id, link_type.id;
select movie_link.linked_movie_id, link_type.id,  min(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.linked_movie_id, link_type.id;
select link_type.id, movie_link.id,  max(movie_link.linked_movie_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by link_type.id, movie_link.id;
select movie_link.id,  max(movie_link.id) from movie_link group by movie_link.id;
select link_type.id, movie_link.movie_id,  min(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by link_type.id, movie_link.movie_id;
select movie_link.movie_id,  sum(movie_link.movie_id) from movie_link group by movie_link.movie_id order by movie_link.movie_id DESC;
select movie_link.id,  max(movie_link.movie_id) from movie_link group by movie_link.id having max(movie_link.movie_id) <= 1840;
select movie_link.linked_movie_id, link_type.id,  min(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.linked_movie_id, link_type.id;
select movie_link.linked_movie_id, link_type.id,  min(movie_link.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.linked_movie_id, link_type.id;
select movie_link.linked_movie_id, link_type.id,  max(movie_link.linked_movie_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select movie_link.id,  max(movie_link.id) from movie_link group by movie_link.id having max(movie_link.id) != 1715;
select movie_link.linked_movie_id, link_type.id,  sum(movie_link.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select link_type.id, movie_link.id,  sum(movie_link.linked_movie_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by link_type.id, movie_link.id;
select movie_link.id, link_type.id,  max(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.id, link_type.id;
select movie_link.movie_id,  sum(movie_link.linked_movie_id) from movie_link group by movie_link.movie_id order by movie_link.movie_id ASC;
select movie_link.linked_movie_id, link_type.id,  sum(movie_link.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.linked_movie_id, link_type.id;
select movie_link.movie_id, link_type.id,  min(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.movie_id, link_type.id;
select movie_link.movie_id, link_type.id,  sum(movie_link.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select movie_link.id, link_type.id,  min(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.id, link_type.id;
select movie_link.movie_id, link_type.id,  max(movie_link.movie_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.movie_id, link_type.id;
select movie_link.id,  min(movie_link.linked_movie_id) from movie_link group by movie_link.id having min(movie_link.linked_movie_id) != 1905;
select movie_link.linked_movie_id, link_type.id,  max(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select link_type.id, movie_link.linked_movie_id,  sum(movie_link.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by link_type.id, movie_link.linked_movie_id;
select movie_link.movie_id,  max(movie_link.id) from movie_link group by movie_link.movie_id order by movie_link.movie_id ASC;
select movie_link.id, link_type.id,  max(movie_link.link_type_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.id, link_type.id;
select movie_link.id,  avg(movie_link.movie_id) from movie_link group by movie_link.id;
select movie_link.id, link_type.id,  max(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.id, link_type.id;
select movie_link.id,  max(movie_link.id) from movie_link group by movie_link.id having max(movie_link.id) <= 1737;
select movie_link.id,  sum(movie_link.linked_movie_id) from movie_link group by movie_link.id;
select link_type.id, movie_link.id,  max(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by link_type.id, movie_link.id;
select movie_link.id,  avg(movie_link.movie_id) from movie_link group by movie_link.id;
select movie_link.movie_id, link_type.id,  sum(movie_link.movie_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select movie_link.id, link_type.id,  sum(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.id, link_type.id;
select movie_link.id,  avg(movie_link.linked_movie_id) from movie_link group by movie_link.id;
select movie_link.id, link_type.id,  max(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.id, link_type.id;
select movie_link.movie_id,  sum(movie_link.link_type_id) from movie_link group by movie_link.movie_id order by movie_link.movie_id DESC;
select link_type.id, movie_link.id,  max(movie_link.linked_movie_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by link_type.id, movie_link.id;
select link_type.id, movie_link.linked_movie_id,  max(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by link_type.id, movie_link.linked_movie_id;
select movie_link.id,  min(movie_link.id) from movie_link group by movie_link.id;
select movie_link.linked_movie_id, link_type.id,  min(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.linked_movie_id, link_type.id;
select movie_link.linked_movie_id, link_type.id,  sum(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select movie_link.movie_id, link_type.id,  max(movie_link.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.movie_id, link_type.id;
select movie_link.movie_id, link_type.id,  sum(movie_link.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select movie_link.id, link_type.id,  max(movie_link.link_type_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.id, link_type.id;
select movie_link.movie_id,  min(movie_link.id) from movie_link group by movie_link.movie_id order by movie_link.movie_id DESC;
select movie_link.movie_id, link_type.id,  min(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.movie_id, link_type.id;
select movie_link.movie_id,  min(movie_link.id) from movie_link group by movie_link.movie_id order by movie_link.movie_id ASC;
select movie_link.id,  max(movie_link.linked_movie_id) from movie_link group by movie_link.id having max(movie_link.linked_movie_id) >= 1913;
select link_type.id, movie_link.movie_id,  min(movie_link.movie_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by link_type.id, movie_link.movie_id;
select movie_link.movie_id,  min(movie_link.linked_movie_id) from movie_link group by movie_link.movie_id order by movie_link.movie_id DESC;
select movie_link.linked_movie_id, link_type.id,  sum(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.linked_movie_id, link_type.id;
select link_type.id, movie_link.movie_id,  min(movie_link.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by link_type.id, movie_link.movie_id;
select movie_link.linked_movie_id, link_type.id,  min(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select movie_link.id,  avg(movie_link.id) from movie_link group by movie_link.id;
select movie_link.movie_id,  max(movie_link.movie_id) from movie_link group by movie_link.movie_id order by movie_link.movie_id ASC;
select movie_link.linked_movie_id,  avg(movie_link.link_type_id) from movie_link group by movie_link.linked_movie_id having avg(movie_link.link_type_id) != 288;
select movie_link.id,  sum(movie_link.id) from movie_link group by movie_link.id having sum(movie_link.id) < 1741;
select movie_link.movie_id, link_type.id,  sum(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select movie_link.id,  avg(movie_link.link_type_id) from movie_link group by movie_link.id;
select movie_link.linked_movie_id, link_type.id,  min(movie_link.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.linked_movie_id, link_type.id;
select movie_link.movie_id,  sum(movie_link.link_type_id) from movie_link group by movie_link.movie_id order by movie_link.movie_id ASC;
select movie_link.movie_id, link_type.id,  sum(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.movie_id, link_type.id;
select movie_link.id,  sum(movie_link.link_type_id) from movie_link group by movie_link.id;
select movie_link.id,  min(movie_link.id) from movie_link group by movie_link.id;
select movie_link.id,  max(movie_link.id) from movie_link group by movie_link.id having max(movie_link.id) >= 1700;
select link_type.id, movie_link.movie_id,  min(movie_link.movie_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by link_type.id, movie_link.movie_id;
select movie_link.linked_movie_id, link_type.id,  max(movie_link.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.linked_movie_id, link_type.id;
select link_type.id, movie_link.movie_id,  min(movie_link.link_type_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by link_type.id, movie_link.movie_id;
select movie_link.linked_movie_id, link_type.id,  sum(movie_link.linked_movie_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.linked_movie_id, link_type.id;
select link_type.id, movie_link.linked_movie_id,  sum(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by link_type.id, movie_link.linked_movie_id;
select movie_link.linked_movie_id, link_type.id,  sum(movie_link.movie_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select movie_link.linked_movie_id, link_type.id,  min(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select movie_link.linked_movie_id, link_type.id,  min(movie_link.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select movie_link.id, link_type.id,  min(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.id, link_type.id;
select movie_link.id, link_type.id,  sum(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.id, link_type.id;
select link_type.id, movie_link.id,  sum(movie_link.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by link_type.id, movie_link.id;
select movie_link.id, link_type.id,  sum(movie_link.movie_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.id, link_type.id;
select movie_link.id, link_type.id,  min(movie_link.linked_movie_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.id, link_type.id;
select movie_link.id, link_type.id,  sum(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.id, link_type.id;
select movie_link.id, link_type.id,  min(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.id, link_type.id;
select movie_link.id,  sum(movie_link.link_type_id) from movie_link group by movie_link.id having sum(movie_link.link_type_id) <= 180;
select movie_link.movie_id,  sum(movie_link.linked_movie_id) from movie_link group by movie_link.movie_id order by movie_link.movie_id ASC;
select movie_link.id, link_type.id,  sum(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.id, link_type.id;
select movie_link.id, link_type.id,  sum(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.id, link_type.id;
select movie_link.id, link_type.id,  sum(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.id, link_type.id;
select movie_link.id,  sum(movie_link.id) from movie_link group by movie_link.id;
select movie_link.linked_movie_id, link_type.id,  sum(movie_link.movie_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.linked_movie_id, link_type.id;
select movie_link.linked_movie_id, link_type.id,  sum(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select link_type.id, movie_link.movie_id,  min(movie_link.movie_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by link_type.id, movie_link.movie_id;
select movie_link.movie_id, link_type.id,  max(movie_link.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select movie_link.id,  avg(movie_link.movie_id) from movie_link group by movie_link.id;
select movie_link.movie_id,  max(movie_link.linked_movie_id) from movie_link group by movie_link.movie_id order by movie_link.movie_id DESC;
select movie_link.id,  max(movie_link.movie_id) from movie_link group by movie_link.id having max(movie_link.movie_id) = 1847;
select movie_link.movie_id, link_type.id,  min(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select movie_link.movie_id, link_type.id,  min(movie_link.link_type_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.movie_id, link_type.id;
select movie_link.movie_id,  sum(movie_link.linked_movie_id) from movie_link group by movie_link.movie_id order by movie_link.movie_id ASC;
select movie_link.movie_id, link_type.id,  max(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select movie_link.id,  sum(movie_link.id) from movie_link group by movie_link.id;
select movie_link.movie_id, link_type.id,  sum(movie_link.movie_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select movie_link.movie_id, link_type.id,  min(movie_link.link_type_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select movie_link.movie_id, link_type.id,  sum(movie_link.linked_movie_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select movie_link.movie_id, link_type.id,  sum(movie_link.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select movie_link.id,  min(movie_link.id) from movie_link group by movie_link.id having min(movie_link.id) < 1725;
select link_type.id, movie_link.movie_id,  sum(movie_link.linked_movie_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by link_type.id, movie_link.movie_id;
select movie_link.movie_id, link_type.id,  min(movie_link.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.movie_id, link_type.id;
select movie_link.movie_id,  sum(movie_link.linked_movie_id) from movie_link group by movie_link.movie_id order by movie_link.movie_id DESC;
select movie_link.id, link_type.id,  max(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.id, link_type.id;
select movie_link.id, link_type.id,  sum(movie_link.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.id, link_type.id;
select movie_link.id,  min(movie_link.linked_movie_id) from movie_link group by movie_link.id having min(movie_link.linked_movie_id) < 1896;
select movie_link.movie_id, link_type.id,  sum(movie_link.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select movie_link.movie_id,  min(movie_link.link_type_id) from movie_link group by movie_link.movie_id order by movie_link.movie_id DESC;
select movie_link.movie_id,  min(movie_link.id) from movie_link group by movie_link.movie_id order by movie_link.movie_id ASC;
select link_type.id, movie_link.linked_movie_id,  min(movie_link.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by link_type.id, movie_link.linked_movie_id;
select movie_link.id,  sum(movie_link.id) from movie_link group by movie_link.id;
select movie_link.id,  min(movie_link.link_type_id) from movie_link group by movie_link.id having min(movie_link.link_type_id) != 294;
select movie_link.id, link_type.id,  min(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.id, link_type.id;
select movie_link.movie_id, link_type.id,  min(movie_link.link_type_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select link_type.id, movie_link.movie_id,  min(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by link_type.id, movie_link.movie_id;
select movie_link.linked_movie_id, link_type.id,  min(movie_link.movie_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select movie_link.id,  sum(movie_link.movie_id) from movie_link group by movie_link.id;
select movie_link.linked_movie_id, link_type.id,  max(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select movie_link.id,  sum(movie_link.movie_id) from movie_link group by movie_link.id;
select movie_link.movie_id,  min(movie_link.id) from movie_link group by movie_link.movie_id order by movie_link.movie_id ASC;
select movie_link.linked_movie_id,  avg(movie_link.id) from movie_link group by movie_link.linked_movie_id having avg(movie_link.id) = 1744;
select movie_link.linked_movie_id, link_type.id,  sum(movie_link.linked_movie_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select link_type.id, movie_link.movie_id,  min(movie_link.linked_movie_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by link_type.id, movie_link.movie_id;
select movie_link.movie_id, link_type.id,  max(movie_link.movie_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select movie_link.movie_id, link_type.id,  min(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select movie_link.id,  max(movie_link.linked_movie_id) from movie_link group by movie_link.id;
select movie_link.movie_id, link_type.id,  min(movie_link.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.movie_id, link_type.id;
select movie_link.movie_id,  sum(movie_link.id) from movie_link group by movie_link.movie_id order by movie_link.movie_id DESC;
select movie_link.id, link_type.id,  sum(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.id, link_type.id;
select movie_link.movie_id,  sum(movie_link.linked_movie_id) from movie_link group by movie_link.movie_id order by movie_link.movie_id ASC;
select movie_link.movie_id,  min(movie_link.link_type_id) from movie_link group by movie_link.movie_id order by movie_link.movie_id DESC;
select movie_link.id,  max(movie_link.id) from movie_link group by movie_link.id;
select movie_link.linked_movie_id, link_type.id,  min(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.linked_movie_id, link_type.id;
select movie_link.linked_movie_id, link_type.id,  max(movie_link.linked_movie_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select movie_link.linked_movie_id, link_type.id,  max(movie_link.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.linked_movie_id, link_type.id;
select movie_link.movie_id, link_type.id,  min(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select movie_link.movie_id, link_type.id,  max(movie_link.movie_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select movie_link.movie_id, link_type.id,  max(movie_link.link_type_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select movie_link.linked_movie_id, link_type.id,  min(movie_link.movie_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select link_type.id, movie_link.linked_movie_id,  sum(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by link_type.id, movie_link.linked_movie_id;
select movie_link.id, link_type.id,  sum(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.id, link_type.id;
select movie_link.id,  sum(movie_link.linked_movie_id) from movie_link group by movie_link.id having sum(movie_link.linked_movie_id) = 1925;
select movie_link.id, link_type.id,  max(movie_link.linked_movie_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.id, link_type.id;
select movie_link.linked_movie_id, link_type.id,  sum(movie_link.movie_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select movie_link.movie_id, link_type.id,  sum(movie_link.link_type_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select movie_link.linked_movie_id, link_type.id,  max(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select movie_link.linked_movie_id, link_type.id,  min(movie_link.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.linked_movie_id, link_type.id;
select link_type.id, movie_link.linked_movie_id,  max(movie_link.linked_movie_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by link_type.id, movie_link.linked_movie_id;
select movie_link.movie_id, link_type.id,  min(movie_link.link_type_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select movie_link.id,  sum(movie_link.link_type_id) from movie_link group by movie_link.id;
select movie_link.id,  sum(movie_link.movie_id) from movie_link group by movie_link.id having sum(movie_link.movie_id) = 1771;
select movie_link.movie_id,  sum(movie_link.link_type_id) from movie_link group by movie_link.movie_id order by movie_link.movie_id DESC;
select link_type.id, movie_link.linked_movie_id,  min(movie_link.movie_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by link_type.id, movie_link.linked_movie_id;
select movie_link.movie_id,  sum(movie_link.id) from movie_link group by movie_link.movie_id order by movie_link.movie_id DESC;
select movie_link.id, link_type.id,  max(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.id, link_type.id;
select movie_link.id,  min(movie_link.linked_movie_id) from movie_link group by movie_link.id;
select movie_link.linked_movie_id, link_type.id,  min(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.linked_movie_id, link_type.id;
select movie_link.id,  sum(movie_link.movie_id) from movie_link group by movie_link.id having sum(movie_link.movie_id) != 1851;
select movie_link.id, link_type.id,  sum(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.id, link_type.id;
select movie_link.linked_movie_id, link_type.id,  max(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select movie_link.linked_movie_id, link_type.id,  min(movie_link.linked_movie_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select movie_link.id,  max(movie_link.linked_movie_id) from movie_link group by movie_link.id;
select movie_link.linked_movie_id, link_type.id,  min(movie_link.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select movie_link.movie_id,  min(movie_link.movie_id) from movie_link group by movie_link.movie_id order by movie_link.movie_id ASC;
select movie_link.id, link_type.id,  sum(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.id, link_type.id;
select movie_link.id, link_type.id,  max(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.id, link_type.id;
select movie_link.movie_id, link_type.id,  max(movie_link.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.movie_id, link_type.id;
select movie_link.linked_movie_id, link_type.id,  sum(movie_link.movie_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select movie_link.id,  min(movie_link.id) from movie_link group by movie_link.id;
select movie_link.id,  max(movie_link.linked_movie_id) from movie_link group by movie_link.id having max(movie_link.linked_movie_id) <= 1943;
select movie_link.movie_id, link_type.id,  min(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.movie_id, link_type.id;
select movie_link.id,  max(movie_link.link_type_id) from movie_link group by movie_link.id;
select link_type.id, movie_link.movie_id,  sum(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by link_type.id, movie_link.movie_id;
select movie_link.id, link_type.id,  min(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.id, link_type.id;
select movie_link.id,  min(movie_link.linked_movie_id) from movie_link group by movie_link.id;
select movie_link.movie_id,  max(movie_link.movie_id) from movie_link group by movie_link.movie_id order by movie_link.movie_id ASC;
select movie_link.movie_id,  sum(movie_link.linked_movie_id) from movie_link group by movie_link.movie_id order by movie_link.movie_id DESC;
select link_type.id, movie_link.movie_id,  max(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by link_type.id, movie_link.movie_id;
select movie_link.linked_movie_id,  avg(movie_link.movie_id) from movie_link group by movie_link.linked_movie_id having avg(movie_link.movie_id) >= 1764;
select movie_link.movie_id, link_type.id,  max(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select movie_link.movie_id,  min(movie_link.id) from movie_link group by movie_link.movie_id order by movie_link.movie_id DESC;
select movie_link.id,  max(movie_link.link_type_id) from movie_link group by movie_link.id having max(movie_link.link_type_id) < 292;
select movie_link.id, link_type.id,  max(movie_link.linked_movie_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.id, link_type.id;
select movie_link.linked_movie_id,  avg(movie_link.id) from movie_link group by movie_link.linked_movie_id having avg(movie_link.id) = 1704;
select movie_link.movie_id,  min(movie_link.link_type_id) from movie_link group by movie_link.movie_id order by movie_link.movie_id ASC;
select movie_link.movie_id,  min(movie_link.id) from movie_link group by movie_link.movie_id order by movie_link.movie_id ASC;
select movie_link.movie_id,  max(movie_link.link_type_id) from movie_link group by movie_link.movie_id order by movie_link.movie_id ASC;
select movie_link.linked_movie_id, link_type.id,  min(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select movie_link.movie_id, link_type.id,  sum(movie_link.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select movie_link.linked_movie_id, link_type.id,  max(movie_link.link_type_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select movie_link.movie_id,  sum(movie_link.id) from movie_link group by movie_link.movie_id order by movie_link.movie_id DESC;
select movie_link.movie_id, link_type.id,  min(movie_link.movie_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.movie_id, link_type.id;
select movie_link.movie_id, link_type.id,  min(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select movie_link.id,  sum(movie_link.link_type_id) from movie_link group by movie_link.id;
select movie_link.linked_movie_id, link_type.id,  sum(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select movie_link.id, link_type.id,  min(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.id, link_type.id;
select movie_link.id, link_type.id,  sum(movie_link.linked_movie_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.id, link_type.id;
select movie_link.linked_movie_id, link_type.id,  sum(movie_link.link_type_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select movie_link.movie_id, link_type.id,  min(movie_link.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select movie_link.id, link_type.id,  sum(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.id, link_type.id;
select movie_link.id,  min(movie_link.linked_movie_id) from movie_link group by movie_link.id;
select movie_link.linked_movie_id, link_type.id,  max(movie_link.movie_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.linked_movie_id, link_type.id;
select movie_link.id,  max(movie_link.link_type_id) from movie_link group by movie_link.id;
select movie_link.id,  sum(movie_link.movie_id) from movie_link group by movie_link.id having sum(movie_link.movie_id) > 1774;
select movie_link.id,  sum(movie_link.linked_movie_id) from movie_link group by movie_link.id having sum(movie_link.linked_movie_id) != 1883;
select link_type.id, movie_link.linked_movie_id,  min(movie_link.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by link_type.id, movie_link.linked_movie_id;
select movie_link.movie_id,  max(movie_link.linked_movie_id) from movie_link group by movie_link.movie_id order by movie_link.movie_id DESC;
select movie_link.id,  avg(movie_link.id) from movie_link group by movie_link.id;
select movie_link.movie_id, link_type.id,  max(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.movie_id, link_type.id;
select link_type.id, movie_link.linked_movie_id,  sum(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by link_type.id, movie_link.linked_movie_id;
select movie_link.id,  sum(movie_link.movie_id) from movie_link group by movie_link.id;
select movie_link.id, link_type.id,  min(movie_link.linked_movie_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.id, link_type.id;
select movie_link.linked_movie_id, link_type.id,  min(movie_link.link_type_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.linked_movie_id, link_type.id;
select movie_link.id, link_type.id,  min(movie_link.movie_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.id, link_type.id;
select link_type.id, movie_link.movie_id,  sum(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by link_type.id, movie_link.movie_id;
select movie_link.linked_movie_id, link_type.id,  min(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select link_type.id, movie_link.id,  min(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by link_type.id, movie_link.id;
select movie_link.id,  min(movie_link.movie_id) from movie_link group by movie_link.id having min(movie_link.movie_id) != 1794;
select movie_link.movie_id, link_type.id,  sum(movie_link.link_type_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.movie_id, link_type.id;
select movie_link.id,  avg(movie_link.id) from movie_link group by movie_link.id;
select movie_link.id,  sum(movie_link.link_type_id) from movie_link group by movie_link.id;
select link_type.id, movie_link.id,  sum(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by link_type.id, movie_link.id;
select movie_link.id,  sum(movie_link.linked_movie_id) from movie_link group by movie_link.id having sum(movie_link.linked_movie_id) >= 1858;
select movie_link.movie_id, link_type.id,  max(movie_link.linked_movie_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select movie_link.movie_id, link_type.id,  min(movie_link.linked_movie_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select movie_link.linked_movie_id, link_type.id,  sum(movie_link.link_type_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select movie_link.linked_movie_id, link_type.id,  max(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select movie_link.id,  sum(movie_link.link_type_id) from movie_link group by movie_link.id;
select movie_link.id,  avg(movie_link.id) from movie_link group by movie_link.id;
select movie_link.id,  max(movie_link.movie_id) from movie_link group by movie_link.id;
select link_type.id, movie_link.id,  max(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by link_type.id, movie_link.id;
select movie_link.id,  sum(movie_link.id) from movie_link group by movie_link.id;
select movie_link.movie_id, link_type.id,  max(movie_link.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.movie_id, link_type.id;
select movie_link.id, link_type.id,  sum(movie_link.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.id, link_type.id;
select movie_link.id,  sum(movie_link.id) from movie_link group by movie_link.id;
select movie_link.id, link_type.id,  max(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.id, link_type.id;
select movie_link.id,  avg(movie_link.movie_id) from movie_link group by movie_link.id;
select link_type.id, movie_link.movie_id,  max(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by link_type.id, movie_link.movie_id;
select link_type.id, movie_link.linked_movie_id,  max(movie_link.link_type_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by link_type.id, movie_link.linked_movie_id;
select movie_link.id,  max(movie_link.linked_movie_id) from movie_link group by movie_link.id;
select movie_link.id, link_type.id,  min(movie_link.link_type_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.id, link_type.id;
select movie_link.id, link_type.id,  max(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.id, link_type.id;
select movie_link.movie_id, link_type.id,  sum(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select movie_link.movie_id, link_type.id,  max(movie_link.link_type_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select movie_link.linked_movie_id, link_type.id,  max(movie_link.linked_movie_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.linked_movie_id, link_type.id;
select movie_link.movie_id, link_type.id,  sum(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select movie_link.id, link_type.id,  min(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.id, link_type.id;
select link_type.id, movie_link.id,  max(movie_link.movie_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by link_type.id, movie_link.id;
select movie_link.id,  sum(movie_link.link_type_id) from movie_link group by movie_link.id having sum(movie_link.link_type_id) = 286;
select link_type.id, movie_link.linked_movie_id,  min(movie_link.linked_movie_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by link_type.id, movie_link.linked_movie_id;
select link_type.id, movie_link.linked_movie_id,  sum(movie_link.linked_movie_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by link_type.id, movie_link.linked_movie_id;
select movie_link.movie_id, link_type.id,  max(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select movie_link.movie_id, link_type.id,  sum(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select movie_link.movie_id,  max(movie_link.id) from movie_link group by movie_link.movie_id order by movie_link.movie_id ASC;
select movie_link.linked_movie_id, link_type.id,  max(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.linked_movie_id, link_type.id;
select movie_link.linked_movie_id, link_type.id,  sum(movie_link.linked_movie_id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.linked_movie_id, link_type.id;
select movie_link.movie_id, link_type.id,  min(movie_link.movie_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select movie_link.id,  sum(movie_link.movie_id) from movie_link group by movie_link.id having sum(movie_link.movie_id) = 1793;
select movie_link.movie_id, link_type.id,  sum(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select movie_link.id,  avg(movie_link.movie_id) from movie_link group by movie_link.id;
select movie_link.movie_id, link_type.id,  max(movie_link.linked_movie_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select link_type.id, movie_link.id,  max(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by link_type.id, movie_link.id;
select movie_link.movie_id, link_type.id,  min(movie_link.link_type_id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.movie_id, link_type.id;
select movie_link.linked_movie_id, link_type.id,  min(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by movie_link.linked_movie_id, link_type.id;
select link_type.id, movie_link.id,  min(link_type.id) from movie_link join link_type on link_type.id=movie_link.link_type_id group by link_type.id, movie_link.id;
select movie_link.linked_movie_id, link_type.id,  sum(link_type.id) from link_type join movie_link on movie_link.link_type_id=link_type.id group by movie_link.linked_movie_id, link_type.id;
